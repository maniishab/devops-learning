pipeline {
    agent any 

    stages {
        stage ('Checkout') {
            steps {
                echo "code pulled from git"
            }
        }
    }

    stage ('Build') {
        steps {
            echo "Build Successful"
        }
    }
}